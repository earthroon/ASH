# ASH-EVE-MCU-CLOSE-R2-PHYS-R1B-CF1-FRESH-RAM36-BOOTSTRAP-FIX

## Fresh RAM36 runtime evidence bootstrap + historical parent contract preservation

### 0. Revision

```text
Patch ID:
ASH-EVE-MCU-CLOSE-R2-PHYS-R1B-CF1-FRESH-RAM36-BOOTSTRAP-FIX

Direct parent:
ASH-EVE-MCU-CLOSE-R2-PHYS-R1B-CF1 SOURCE-LOAD-WITNESS-SCOPE-FIX

Observed physical blocker:
PHYS_R1_PRODUCTION_ERROR:BASETRAIN_RAM36_PARENT_INVENTORY_RECEIPT_REQUIRED

Algorithm / Adam / Muon / R13 math change: NO
Historical resume relaxation: NO
```

### 1. Failure classification

The real WGPU native bootstrap succeeded before this failure. Therefore the blocker is downstream of FreshGenesis input admission, Native CF1, self-materialized config, R4/R2 profile admission and native device bootstrap.

The remaining conflict was in `pipeline.rs`: `admit_ram36_process_budget_authority=true` unconditionally required `ram36_parent_inventory_receipt_path` to build `HostProcessRamBudget`.

FreshGenesis explicitly forbids that historical receipt.

### 2. Fresh RAM36 authority law

FreshGenesis shall not fabricate a historical RAM36 parent inventory.

Instead:

```text
36 GiB OS/process cap
  + exact first dataset-build measurement
  + canonical model-derived Adam state geometry
  + current CF1 binary identity
  -> FreshGenesis RAM36 runtime evidence envelope
  -> HostProcessRamBudget
```

Receipt semantics must expose:

```text
budgetEvidenceOrigin = FRESH_GENESIS_RUNTIME_BOOTSTRAP_EXACT_INVENTORY
historicalParentInventoryReused = false
parentInventoryReceiptSha256 = ""
```

Historical source keeps:

```text
budgetEvidenceOrigin = HISTORICAL_PARENT_EXACT_INVENTORY
historicalParentInventoryReused = true
```

### 3. Dataset bootstrap law

Because a fresh lineage has no earlier dataset-build peak, the first dataset window is physically built under the already-installed exact 36 GiB process cap while `build_batches_from_manifest_split_with_ram_inventory(...)` measures ownership and process peaks.

Required before RAM36 budget publication:

```text
process observation supported
raw payload exact
opaque nested allocator count = 0
managed build bytes > 0
resident batch bytes > 0
observed private peak <= 36 GiB
```

The resulting dataset report becomes current fresh evidence. It is not labeled historical parent evidence.

### 4. Already-materialized dataset adoption

Fresh bootstrap occurs after the first dataset batches physically exist. The RAM36 ledger therefore adopts that exact resident allocation with a dedicated already-materialized transition.

It must not reserve the same bytes again before adoption, which would double-charge the process projection.

### 5. Adam geometry authority

Fresh Adam M/V bound is derived from:

```text
canonical_inventory_from_model_spec(spec)
```

using exact F32 bytes for all 201 canonical parameters. This must later equal the packed R1A `adam_m_pack_bytes` and `adam_v_pack_bytes`; the existing scheduler equality check remains active.

No optimizer state is regenerated by this correction.

### 6. Muon / optimizer transient law

FreshGenesis has no historical optimizer transient peak. Route-exact HiMuon transient admission therefore receives:

```text
historicalParentPeakPresent = false
parent segment peak = 0
parent serialization peak = 0
```

The route-exact topology bound calculated from the live optimizer routing remains the admission authority.

Historical paths still require positive historical parent peaks and preserve stale-parent-retirement evidence.

### 7. Historical contract preservation

The following remain unchanged for non-fresh N8:

```text
ram36_parent_inventory_receipt_path required
HostProcessRamBudget::load_parent_inventory(...)
HostProcessRamBudget::from_parent_receipt(...)
immutable N2 RAM36 exact-retry validation
historical positive transient peak checks
```

### 8. Source scope

```text
ADD 0
MOD 4
DEL 0
source-delta digest:
bbf3c34c661adb4849789901cd751f98f953e63633e9ed3a782785aa58ab0baa
```

Changed files:

```text
crates/base_train/src/ram36_process_budget.rs
crates/base_train/src/ram36_himuon_coresident_route_exact_optimizer_transient_bound_r1.rs
crates/base_train/src/pipeline.rs
crates/base_train/src/production_multistep_loop_accumulation8_scheduler.rs
```

No Cargo manifest or lockfile change.

### 9. Code-only bake

```text
Full:
ASH_PASS3_EVE_MCU_CLOSE_R2_PHYS_R1B_CF1_FRESH_RAM36_BOOTSTRAP_FIX_CODE_ONLY.zip
SHA-256: 67136b4919255fff07fedf2a961dda5efb930b84eff3f5fcecdc67f3102a2fea
Files: 8423
CRC: PASS

Overlay:
ASH_EVE_MCU_CLOSE_R2_PHYS_R1B_CF1_FRESH_RAM36_BOOTSTRAP_FIX_OVERLAY_CODE_ONLY.zip
SHA-256: 5bad28f2c42376f24134365e07b1ca311cea10fc142d9f6f7097c1add5214c56
Files: 4
CRC: PASS
```

### 10. Validation boundary

Current evidence:

```text
SOURCE: PASS
STATIC SCOPE: PASS
ZIP CRC: PASS
COMPILE: PENDING USER MACHINE
NATIVE CF1: RESEAL REQUIRED
A/B/C PHYSICAL: PENDING
PERFORMANCE: UNKNOWN
```

### 11. Final law

FreshGenesis RAM36 begins from current physical runtime evidence, not a fabricated N2/N8 parent receipt. The first exact dataset measurement under the hard 36 GiB cap establishes the fresh budget envelope; existing RAM-Adam, Muon, resident-weight and successor reservations then continue through the same HostProcessRamBudget authority. Historical RAM36 resume validation remains unchanged.