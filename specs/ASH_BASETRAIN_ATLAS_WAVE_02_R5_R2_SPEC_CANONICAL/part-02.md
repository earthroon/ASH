production-model shape correctness
GQA correctness
external RoPE convention correctness
```

These must be recorded as:

```text
EvidenceAdmissionHold
```

## 8.3 Required classification object

```json
{
  "physicalExecution": "PASS_OBSERVED",
  "numericalFixtureExecution": "PASS_OBSERVED",
  "artifactLineage": "HOLD_PENDING_R5_R2",
  "proofLedgerStrength": "HOLD",
  "noReceiptOverclaimClaim": "REVOKED_PENDING_REQUALIFICATION",
  "productionModelGeometry": "UNPROVEN",
  "gqaGeometry": "UNPROVEN",
  "ropeConvention": "UNPROVEN",
  "r6Admission": "BLOCKED"
}
```

R5-R2 may change only `artifactLineage` to `PASS`.

All other HOLD or UNPROVEN states remain unchanged.

---

# 9. No in-place receipt mutation

The following files are immutable historical artifacts:

```text
ASH_BASETRAIN_ATLAS_WAVE_02_R5_OVERLAY_RECEIPT.json
ASH_BASETRAIN_ATLAS_WAVE_02_R5_STATIC_CHECKS.txt
ASH_BASETRAIN_ATLAS_WAVE_02_R5_CODE_BAKE.md
ASH_BASETRAIN_ATLAS_WAVE_02_R5_R1_HOST_STAGING_TYPE_CLOSURE.md
R5 body ZIP
R5 overlay ZIP
R5-R1 body ZIP
R5-R1 overlay ZIP
```

R5-R2 must not modify them.

The patch adds successor evidence files only.

Required guard:

```text
historical artifact pre-hash
apply R5-R2
historical artifact post-hash
pre-hash == post-hash for every historical artifact
```

Any historical mutation is a hard failure.

---

# 10. R5-R2 output artifact set

Required new files:

```text
ASH_BASETRAIN_ATLAS_WAVE_02_R5_R2_SPEC.md
ASH_BASETRAIN_ATLAS_WAVE_02_R5_R1_OVERLAY_RECEIPT.json
ASH_BASETRAIN_ATLAS_WAVE_02_R5_R1_STATIC_CHECKS.txt
ASH_BASETRAIN_ATLAS_WAVE_02_R5_R1_FULL_PACKAGE_MANIFEST.json
ASH_BASETRAIN_ATLAS_WAVE_02_R5_R1_FULL_PACKAGE_MANIFEST.sha256
ASH_BASETRAIN_ATLAS_WAVE_02_R5_R2_EVIDENCE_RECLASSIFICATION.json
ASH_BASETRAIN_ATLAS_WAVE_02_R5_R2_CODE_BAKE.md
ASH_BASETRAIN_ATLAS_WAVE_02_R5_R2_OVERLAY_RECEIPT.json
ASH_BASETRAIN_ATLAS_WAVE_02_R5_R2_STATIC_CHECKS.txt
```

No runtime GPU evidence is regenerated by R5-R2.

No shader or Rust compute path is modified.

If implementation adds a receipt-generation utility, it must be a host-side package inspection tool with no model runtime authority.

---

# 11. R5-R2 physical gate

R5-R2 uses a package-integrity gate, not a GPU gate.

Suggested binary:

```text
ash_basetrain_atlas_wave_02_r5_r2_lineage_gate
```

Inputs:

```text
--r5-parent-body-zip
--r5-parent-overlay-zip
--r5-r1-child-body-zip
--r5-r1-child-overlay-zip
--existing-r5-overlay-receipt
--physical-pass-log
--output-dir
```

The gate must inspect ZIP contents directly.

It must not trust an already extracted directory without also binding that directory to the supplied ZIP digest and extraction manifest.

---

# 12. Required positive assertions

The following are real assertions, not a required arbitrary count floor.

```text
POS-LINEAGE-001  R5 body ZIP digest exact
POS-LINEAGE-002  R5 overlay ZIP digest exact
POS-LINEAGE-003  R5-R1 body ZIP digest exact
POS-LINEAGE-004  R5-R1 overlay ZIP digest exact
POS-LINEAGE-005  R5-R1 changed-file count equals 3
POS-LINEAGE-006  R5-R1 changed-file set exact
POS-LINEAGE-007  one added path exact
POS-LINEAGE-008  two modified paths exact
POS-LINEAGE-009  removed path count zero
POS-LINEAGE-010  full child package enumerated
POS-LINEAGE-011  full tree digest recomputable
POS-LINEAGE-012  existing 27-entry receipt checked
POS-LINEAGE-013  matching entry count 24
POS-LINEAGE-014  stale entry count 3
POS-LINEAGE-015  inherited stale entry count 1
POS-LINEAGE-016  expected child-delta stale count 2
POS-LINEAGE-017  undeclared child mutation count zero
POS-LINEAGE-018  historical artifact mutation count zero
POS-LINEAGE-019  physical PASS log digest recorded
POS-LINEAGE-020  evidence admission remains HOLD outside lineage scope
```

Each assertion must be recorded before final admission.

---

# 13. Required negative controls

R5-R2 must execute distinct package-lineage mutations.

Minimum canonical set:

```text
NEG-LINEAGE-001  wrong R5 body ZIP digest
NEG-LINEAGE-002  wrong R5 overlay ZIP digest
NEG-LINEAGE-003  wrong R5-R1 body ZIP digest
NEG-LINEAGE-004  wrong R5-R1 overlay ZIP digest
NEG-LINEAGE-005  missing expected added file
NEG-LINEAGE-006  missing expected modified file
NEG-LINEAGE-007  undeclared extra modified file
NEG-LINEAGE-008  undeclared removed file
NEG-LINEAGE-009  parent SHA substituted with child SHA
NEG-LINEAGE-010  child SHA substituted with parent SHA
NEG-LINEAGE-011  path separator ambiguity
NEG-LINEAGE-012  normalized path collision
NEG-LINEAGE-013  duplicate changed-file entry
NEG-LINEAGE-014  changed-file ordering drift
NEG-LINEAGE-015  stale receipt mismatch hidden by count-only check
NEG-LINEAGE-016  in-place parent receipt mutation
NEG-LINEAGE-017  inherited stale misclassified as R5-R1 delta
NEG-LINEAGE-018  expected child delta misclassified as corruption
NEG-LINEAGE-019  physical PASS promoted to evidence PASS
NEG-LINEAGE-020  no-overclaim claim retained without requalification
```

Each mutation must have a distinct mutation digest.

Duplicating the same mismatch under different IDs is forbidden.

---

# 14. Static checks

Required static checks:

```text
existing R5 receipt file SHA unchanged
existing R5 static checks SHA unchanged
R5-R1 closure SHA unchanged
parent body ZIP SHA exact
child body ZIP SHA exact
new receipt schema exact
new receipt passScope exact
new evidence classification contains R6Admission=BLOCKED
new changed-file list contains repair script
new changed-file list excludes R5 CODE_BAKE as R5-R1 delta
full package manifest covers all files
```

The static-check output must include computed values, not only `PASS` strings.

---

# 15. Spine blockers discovered after R5 physical PASS

R5-R2 is not permitted to conceal deeper execution-graph risks.

The following are mandatory successor blockers.

## 15.1 Tensor geometry authority is duplicated

Current geometry is independently declared in:

```text
fixture_spec()
BaseTrainAtlasWave02R5Params literal construction
BaseTrainAtlasWave02R5ResidentWeights::validate()
```

Observed fixture values:

```text
vocab_size        32
hidden_size        8
num_heads          2
num_kv_heads       2
head_dim           4
rope_theta     10000
```

Required future direction:

```text
checkpoint header/config
  -> ModelSpec
  -> validated ModelGeometry
  -> Params
  -> resident tensor shape validation
  -> shader parameters
```

No downstream layer may repeat the geometry as independent literals.

## 15.2 GQA is currently rejected by construction

