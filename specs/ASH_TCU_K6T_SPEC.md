# ASH TCU K6T

Bound route stability window after K6S apply monitor.

Required K6S PASS with monitor correctness, monitor timing, rollback probe, fallback route probe, and post-probe route state all valid.

Verifies the explicitly bound K6P GPU row-major emit TensorCube candidate route across a short stability window with correctness windows, timing windows, timing variance guard, route state window probe, rollback readiness window probe, fallback route window probe, and known CLI echo mismatch carry-forward.

Does not execute rollback, silently adopt, overwrite production GEMM, or claim external performance.

PASS PASS_ASH_TCU_K6T_BOUND_ROUTE_STABILITY_WINDOW_NO_PRODUCTION_REPLACEMENT_SEAL.
