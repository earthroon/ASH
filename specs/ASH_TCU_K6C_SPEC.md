# ASH-TCU-K6C

Runtime candidate ABI shadow gate after K6B.

Required: K6B PASS.

Shape: M64, N64, K32.

Route: candidate registered, default off, shadow only.

Fallback: required for all failures.

Output owner: existing runtime path.

Candidate owner: K6C shadow receipt only.

Guards: runtime splice closed, production replacement closed, performance claim closed.

PASS marker: PASS_ASH_TCU_K6C_RUNTIME_CANDIDATE_ABI_SHADOW_PARITY_GATE_SEAL.
