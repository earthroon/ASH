# ASH TCU K6ZS

Limited Production-Shadow Promotion Review / K6ZR Runtime Evidence Source Classification / No Production Replacement No Default Adoption Seal.

Required prior PASS: ASH-TCU-K6ZR with limited production-shadow runtime monitor executed, shadow scope limited_production_shadow_canary, shadow mode observation_only, 8 health windows, 16 seeds per window, 128 total probes, valid shadow observation health window, valid production parity divergence matrix, valid output non-commit guard, valid default route firewall, valid user-visible output firewall, valid post-bind replay, valid diff stability, valid timing stability, valid scope isolation, valid fallback readiness, valid rollback rehearsal, valid production replacement block, and no shadow output commit, no default adoption, no user-visible adoption, no production replacement, no production route state change, no global default route change, no broad rollout, no rollback execution, and no performance claim.

K6ZS reviews K6ZR evidence and classifies the source of each runtime evidence lane as runtime_measured, prior_receipt_replayed, contract_generated, static_expected, or unknown. Unknown or static_expected evidence must not be promoted as apply-ready. K6ZS emits a promotion review verdict and recommends K6ZT measurement source rebind when runtime measurement provenance is not fully closed.

K6ZS does not commit shadow outputs, use shadow outputs as default, use shadow outputs for user-visible results, replace production GEMM, change production route, change the global default route, broaden rollout, execute rollback, open runtime splice, or make performance claims.

PASS marker: PASS_ASH_TCU_K6ZS_LIMITED_PRODUCTION_SHADOW_PROMOTION_REVIEW_NO_PRODUCTION_REPLACEMENT_SEAL.

Recommended next patch: ASH-TCU-K6ZT_RUNTIME_MEASUREMENT_SOURCE_REBIND_NO_STATIC_METRIC_PROMOTION.
