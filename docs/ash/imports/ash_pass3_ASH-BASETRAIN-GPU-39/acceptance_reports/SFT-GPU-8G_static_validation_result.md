# SFT-GPU-8G Static Validation Result

Command:

```bash
python tools/validate_sft_gpu_8g_static.py
```

Result:

```text
PASS count: 18
[PASS_STATIC] SFT-GPU-8G lm_head vocab atlas grouped parallel projection / streaming CE seal
```

Runtime status: `PENDING_RUNTIME` because this environment cannot run Cargo/WGPU.
