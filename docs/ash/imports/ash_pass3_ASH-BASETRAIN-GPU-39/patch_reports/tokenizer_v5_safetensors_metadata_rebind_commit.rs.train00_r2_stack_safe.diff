--- tokenizer_v5_safetensors_metadata_rebind_commit.rs.before
+++ tokenizer_v5_safetensors_metadata_rebind_commit.rs.after
@@ -264,7 +264,11 @@
 
 fn sha256_reader<R: Read>(reader: &mut R) -> Result<String> {
     let mut hasher = Sha256::new();
-    let mut buf = [0u8; 1024 * 1024];
+    // Keep the streaming buffer on the heap. Windows debug binaries can have
+    // a small default stack, and a 1 MiB stack array was enough to overflow
+    // before the R2 dry-run could even write the report. This still streams
+    // the checkpoint payload and never decodes tensor bytes.
+    let mut buf = vec![0u8; 64 * 1024];
     loop {
         let n = reader.read(&mut buf)?;
         if n == 0 {
