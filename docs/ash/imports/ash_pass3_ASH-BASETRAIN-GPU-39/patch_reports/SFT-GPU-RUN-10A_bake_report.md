# SFT-GPU-RUN-10A Bake Report

Patched A-SFT hidden source binding without disabling the full checkpoint teacher guard.

Main config is now train_from_features and points at the feature store manifest path generated by the companion native_dump config.
