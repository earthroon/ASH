# 16AI-QW-38G-R6A-R12A-R11A-R2 — User Case Schema Import Repair

## Status
STATIC_VALIDATION_PASS

## Scope
Repairs R11A user case import so wrapper-object, raw-array, and single-case input shapes are normalized before authoring. The patch preserves `case_id`, `prompt_text`, `target_attractor_relevance`, `risk_axis`, `expected_behavior`, and `pass_condition`, then recomputes strict `validation_ready` and min-count readiness.

## Guard
- No prompt text is generated by the runner.
- Missing fields are recorded as gap codes.
- `overall_validation_case_set_ready=true` requires at least 3 strict-ready prompt variants and 3 strict-ready negative controls.
- Canonical R11A case/readiness files are rewritten from repaired SSOT.
- Runtime default apply remains false.
- Checkpoint/tokenizer/safetensors/lm_head/final_norm/ban mask are not modified.
