# ASH-FT-05-R1 Export Surface Fix

This patch rebinds the FT-04 type export surface so FT-05 can import FT-04 upload plan, upload receipt, and GPU snapshot schemas from the crate root.

No compute, optimizer, or candidate write behavior is changed.
