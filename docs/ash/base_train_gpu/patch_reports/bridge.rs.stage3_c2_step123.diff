--- /mnt/data/stage3c2s12_work/crates/lora_train/src/bridge.rs	2026-04-19 13:34:06.000000000 +0000
+++ /mnt/data/stage3c2s123_work/crates/lora_train/src/bridge.rs	2026-04-19 13:41:18.247733645 +0000
@@ -142,28 +142,45 @@
                     record_ids,
                 ) {
                     Ok(slot_id) => {
-                        let _ = native.poll_trace_slot(slot_id)?;
-                        match native.try_collect_ready_module_trace(slot_id)? {
-                            Some((_meta, captures)) => {
-                                println!(
-                                    "[bridge][module_local][runtime] native capture ok target_key={} batch={} max_seq_len={} slot_id={}",
-                                    target_key,
-                                    lengths.len(),
-                                    max_seq_len,
-                                    slot_id,
-                                );
-                                Ok(captures)
+                        let mut ready = false;
+                        for _ in 0..4 {
+                            if native.poll_trace_slot(slot_id)? {
+                                ready = true;
+                                break;
                             }
-                            None => {
-                                println!(
-                                    "[bridge][module_local][runtime] native trace slot pending target_key={} batch={} max_seq_len={} slot_id={} -> fallback",
-                                    target_key,
-                                    lengths.len(),
-                                    max_seq_len,
-                                    slot_id,
-                                );
-                                fallback.capture_module_io_padded_batch(token_ids, lengths, max_seq_len, target_key)
+                        }
+                        if ready {
+                            match native.try_collect_ready_module_trace(slot_id)? {
+                                Some((_meta, captures)) => {
+                                    println!(
+                                        "[bridge][module_local][runtime] native capture ok target_key={} batch={} max_seq_len={} slot_id={}",
+                                        target_key,
+                                        lengths.len(),
+                                        max_seq_len,
+                                        slot_id,
+                                    );
+                                    Ok(captures)
+                                }
+                                None => {
+                                    println!(
+                                        "[bridge][module_local][runtime] native trace slot ready-without-collect target_key={} batch={} max_seq_len={} slot_id={} -> fallback",
+                                        target_key,
+                                        lengths.len(),
+                                        max_seq_len,
+                                        slot_id,
+                                    );
+                                    fallback.capture_module_io_padded_batch(token_ids, lengths, max_seq_len, target_key)
+                                }
                             }
+                        } else {
+                            println!(
+                                "[bridge][module_local][runtime] native trace slot still pending after poll window target_key={} batch={} max_seq_len={} slot_id={} -> fallback",
+                                target_key,
+                                lengths.len(),
+                                max_seq_len,
+                                slot_id,
+                            );
+                            fallback.capture_module_io_padded_batch(token_ids, lengths, max_seq_len, target_key)
                         }
                     }
                     Err(err) => {
