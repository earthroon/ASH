# ASH-FT-12 Acceptance

PASS requires metadata-only temporary candidate slot bind, required metadata handle lookup, backend compatibility receipt, no-decode guard, and all runtime mutation/generation guards false.
