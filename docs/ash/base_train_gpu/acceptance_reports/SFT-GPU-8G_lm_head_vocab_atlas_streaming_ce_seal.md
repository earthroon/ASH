# SFT-GPU-8G Acceptance

## lm_head vocab atlas grouped parallel projection / streaming CE seal

Status: `PASS_STATIC / PENDING_RUNTIME`

## SSOT

- Train-from-features entry: `crates/lora_train/src/sft_feature_store.rs`
- Vocab atlas trainer: `crates/lora_train/src/lm_head_vocab_atlas.rs`
- Config contract: `configs/ash_ko_short_sft_lm_head_lora_v1b_train_from_features.json`
- Legacy full-buffer guard: `crates/lora_train/src/model.rs`

## Confirmed static gates

- `lm_head_vocab_atlas.enabled=true`
- `projection=vocab_atlas_tiled`
- `streaming_cross_entropy=true`
- `forbid_full_vocab_buffer=true`
- `forbid_full_logits_buffer=true`
- `forbid_trainable_module_from_base_for_lm_head=true`
- `target_key=lm_head` dispatches to `run_lm_head_vocab_atlas_lora_train()`
- Legacy `TrainableModuleLoraAdapter::from_base_weight(lm_head)` panics with an 8G-specific guard before allocating the 395337728-byte tensor.
- Vocab tiles are generated by `vocab_tile_size` and projected as tile tensors.
- Streaming logsumexp CE is implemented without a full `[tokens, vocab]` logits buffer.
- Target-logit tile capture uses `target_seen` / `target_logit` guards.
- Report writer emits `vocab_atlas_train_report.json` and `.md`.

## Runtime command

```powershell
cargo run -p lora_train --bin lora_train -- configs/ash_ko_short_sft_lm_head_lora_v1b_train_from_features.json 1
```

## Expected runtime markers

```text
[lora_train][lm_head_vocab_atlas] enabled=true projection=vocab_atlas_tiled
[lora_train][lm_head_vocab_atlas] vocab_size=48259 hidden=2048 tile_size=2048 tile_count=24
[lora_train][lm_head_vocab_atlas] PASS streaming_ce=true full_vocab_buffer=false full_logits_buffer=false
```

## Runtime acceptance still pending

The current bake environment has no `cargo` binary and no top-level workspace `Cargo.toml`, so Rust compile/runtime execution was not performed here.
